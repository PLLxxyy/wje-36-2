做一个数据可视化的服务器资源监控大屏。

页面是一个全屏的大屏看板，不需要登录。左侧显示多台服务器的 CPU 使用率折线图，每台一条不同颜色的线，实时滚动更新。右侧上面是内存使用率的仪表盘，像汽车速度表一样指针转动，下面显示磁盘使用率的横向进度条。

中间区域是网络流量图，上行和下行用两种颜色区分，面积图形式。底部有一排服务器卡片，每台显示主机名、在线状态、运行时长和简要指标。数据定时刷新，有异常的服务器整个卡片变红闪烁提醒。